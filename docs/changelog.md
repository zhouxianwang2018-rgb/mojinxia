# 摸金虾 更新日志

---

## v1.0.1 — 2026-06-03 · 开发工具链

首个补丁版本，建立完整的版本管理和迭代流程。

### 新增

- **DEVELOPMENT.md**：开发手册（迭代节奏 / 版本管理规范 / 回滚流程）
- **sync-to-git.sh**：一键同步脚本（Hermes → GitHub，含自动备份）
- **issues.json**：待修复清单模板，每日盯盘写入，每周六排期

### 改进

- `.backup/` 备份范围扩展：CRON prompt + issues.json + skill + 策略文件（仅本地）
- `.backup/*/state/` 加入 `.gitignore`，策略文件不进公开仓库
- `README.md` 增加 DEVELOPMENT.md 索引

---

## v1.0 — 2026-06-02 · 首个正式版本

经过 5/15 至今 18 天的持续迭代，架构规范化完成，正式发布首个正式版本。

### 交付层精简

- **午盘执行**：从 20 行 Markdown 大报告砍到 ≤5 行。去大盘/板块/账户重复内容，只报风险等级 + 仓位 + 操作。始终推送。
- **尾盘执行**：从 `baoyu-infographic` 报纸图片改为结构化纯文本。
- **引擎二**：从静默到正式上线，交付 📰 排名报纸图片。

### 数据流规范化

- **`context_from` 全部清零**：全部 7 个 CRON 不再使用上下文注入。
- **策略文件成为唯一数据总线**：引擎一 → `candidate_pool.engine1` → 引擎二读并写 `engine2.ranked` → 尾盘/复盘读取。
- **引擎一**：新增 Step 2 写入策略文件，`deliver` → `local`。
- **引擎二**：新增 Step 0 读引擎一结果，排名后写回策略文件。
- **尾盘执行**：新增 Step 0b「读取候选池」，计算双引擎交叉命中。

### 设计原则确立

策略文件是唯一真相源 · 单写者无并发 · 硬约束前置 · 引擎结果策略化 · 报纸留给锚点 · 侦察不重复

### 产品设计文档独立

`products/摸金虾/` 目录创建，设计文档（changelog / roadmap / architecture）从 skill 迁移到独立产品目录。

---

## v0.4 — 2026-06-01 · 侦察收敛 v5 + 报纸图片

### 侦察层与执行层分离

提出三层模型：策略层（收盘复盘 + 盘前定调）→ 侦察层（午后侦察 13:16）→ 执行层（午盘 + 尾盘）。侦察权收敛到午后一个节点，执行层只读快照。

### 午后侦察升级

从「只能写文本预警」升级为输出结构化 `market_snapshot` JSON + 可追加 `afternoon_stop_loss` / `afternoon_emergency` scenario。

### 全部 CRON 重命名

| 旧名 | 新名 |
|------|------|
| 摸金虾-盘前分析 | 摸金虾·盘前定调 |
| 摸金虾-午盘检查 | 摸金虾·午盘执行 |
| 摸金虾-午后开盘检查 | 摸金虾·午后侦察 |
| 早期突破扫描 | 摸金虾·引擎一扫描 |
| 摸金虾-引擎二选股 | 摸金虾·引擎二排名 |
| 摸金虾-尾盘检查 | 摸金虾·尾盘执行 |

### 报纸图片交付

盘前定调 + 尾盘执行改为报纸风格 PNG（Playwright 渲染），标题「摸金虾·盘前早参」「摸金虾·尾盘战报」。图片生成统一放各 CRON 最后一步。

### 平衡计分卡 + Scenario 审计

- 平衡计分卡：独立子系统，四维评估（收益/风控/执行/进化），嵌入收盘复盘
- Scenario 审计：首次审计触达率 21%、进攻型 0/5，建立审计驱动的改进循环

---

## v0.3 — 2026-05-29 · 量化目标 + 午后侦察 + 三体联动

### 量化目标设定

年目标 ¥2,000,000（+100%），月 ≥15%。月复合路径：5月底 ~111万 → 10月 223万。

### 午后侦察上线

新增 13:16 午后侦察 CRON，填补 10:00→14:20 四小时真空。

### 三体联动架构

```
09:25 盘前 → market_context
14:20 引擎一 → candidate_pool.engine1
14:25 引擎二 → 交叉排名 → engine2.ranked
15:05 收盘复盘 → 自动生成次日 scenarios
```

### 其他

- `risk_state` 空仓日 P&L=0 自动熔断重置
- `moni_check_trades.py` 双重验证上线
- `next_trading_day.py` 辅助脚本

---

## v0.2 — 2026-05-28 · 策略链 + 风控 v4

### 策略链文件系统

从单一 `strategy.json` 重构为每日独立策略文件 + 状态机：

```
策略链: pending → active → closed → archived
```

### 加载协议

所有 CRON 统一 Step -1（安全校验）+ Step 0（加载策略链）+ Step 0a（硬约束检查）。

### 风控 v4

`calc_risk_state.py` 硬编码风险计算，禁止 LLM 手动修正。五级风控 + 硬约束。

### orders API 降级

`moni_check_trades.py` 双重验证（orders API + 快照对比）。

### 新增 CRON

午后开盘检查（13:00）+ 早期突破扫描（14:20）。总计 4→6 个 CRON。

---

## v0.1 — 2026-05-15 · 初版上线

首个可运行版本。4 个独立 CRON：盘前分析（09:25）、午盘检查（10:00）、尾盘检查（14:30）、收盘复盘（15:05）。妙想 API + 模拟交易 API 集成，QQ bot 消息交付。不存在策略链概念，各 CRON 独立运行。
